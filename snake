<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<title></title>
	<style type="text/css">
		#container{
			background-color: grey;
			
		}
		.snake{
			background-color: red;
		}
		.row{
			width: 440px;	
		}
		.gridsquare {
			width:20px;
			height: 20px;
			border: 1px solid black;
			display: inline-block;
		}
	</style>


  <script language="javascript"> 

		var field = {
			width : 20,
			height : 20,
			holder : "",
			speed: 1000
		};


		var snake = {
			size : 1,
			color : 'red',
			coordX: 4,
			coordY: 5
		};


	    function genDivs(v){ 
	      var e = document.body; // whatever you want to append the rows to: 
	      for(var i = 0; i < v; i++){ 
	        var row = document.createElement("div"); 
	        row.className = "row"; 
	        for(var x = 1; x <= v; x++){ 
	            var cell = document.createElement("div"); 
	            cell.className = "gridsquare"; 
	            row.appendChild(cell); 
	        } 
	        e.appendChild(row); 
	      } 
	    	init();
	    };

	     var init = function(){
	    	 var y = document.getElementsByClassName('row');
	    	 var body = y[snake.coordY].children[snake.coordX];
	    	 body.style.backgroundColor = snake.color; 
	    }; 

	    var movement = function(){
	    	setInterval(function(){
	    		snake.coordX +=1;
	    		if (snake.coordX > field.width){
	    			alert("You lose");
	    			console.log (snake.coordX)
	    			return;
	    		}
	    		init();
	    	}, field.speed)
	    }

	    movement();

  </script> 
</head> 
<body> 
    <input type="button" onclick="genDivs(field.width)" value="create field"> 
     
</body> 
</html> 

<!--

/*
	<script type="text/javascript">
		var field = {
			width : 20,
			height : 20,
			holder : "",
		};

		var render = function (){
			for (var i = 0; i < field.width; i++){
				var block = document.createElement("div");
				var node = document.createTextNode(field.holder);
				block.appendChild(node)                     // Append the text to <div>
				var cell = document.getElementById("container")      // Get the parent element to insert a new node
				cell.insertBefore(block, cell.childNodes[0]); 
			}
		};
		render();
	</script>
</body>
</html>

*/

-->
